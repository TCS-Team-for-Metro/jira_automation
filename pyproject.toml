[project]
name = "web-automation"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "ashish-dake-metro", email = "ashish.dake@metro-external.digital" }
]
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1.5",
    "playwright>=1.62.0",
]

[project.scripts]
web-automation = "web_automation:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
